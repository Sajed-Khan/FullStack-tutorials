import './App.css';
import React from 'react';
import Note from './components/Note';

const App = (props) => {
  const {notes} = props

  const result = notes.map(note=>note.id)
  console.log(result) //maps create arrays and will only work with arrays

  console.log('App works')

  return (
    <div>
      <h1>Notes</h1>
      <ul>
        {/*notes.map(note => <li key = {note.id}>{note.content}</li>) first way*/}
        {/*notes.map((note,i)=><li key = {i}>{note.content}</li>)  second way */}
        {notes.map(note=> <Note key={note.id} note={note}/>)}
      </ul>
    </div>
    //must have unique key value to determine how to update the view generated by a component when it is rendered
  )
}

export default App
